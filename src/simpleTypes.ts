//TypeScript Simple Types

// Explicit
// Implicit

//Explicit - writing out the type:
let explicit: string = "Dylan";
//Implicit - TypeScript will "guess" the type, based on the assigned value:
let implicit = "Dylan";

export { explicit, implicit };
